services:

  proxy:
    container_name: psc-atlas
    build:
      target: proxy-dev
      args:
        UID: "${UID:-1000}"
        GID: "${GID:-1000}"

  backend:
    container_name: backend
    build:
      context: backend
      dockerfile: ../Dockerfile
      target: backend-dev
      args:
        UID: "${UID:-1000}"
        GID: "${GID:-1000}"
    volumes:
      - ./backend:/home/psc-atlas/backend

  frontend:
    container_name: frontend
    build:
      context: frontend
      dockerfile: ../Dockerfile
      target: frontend-dev
      args:
        UID: "${UID:-1000}"
        GID: "${GID:-1000}"
    volumes:
      - ./frontend:/home/psc-atlas/frontend
