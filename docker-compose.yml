name: psc-atlas

services:

  psc-atlas:
    build:
      context: .
      target: prod
    volumes:
      - project-vol:/home/psc-atlas/vol
    ports:
      - 127.0.0.1:3320:8000

volumes:
  project-vol:
